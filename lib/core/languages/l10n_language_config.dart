import 'package:flutter/material.dart';

class L10nLanguageConfig {
  static final all = [
    const Locale('en'), // English
    const Locale('vi'), // Vietnamese
    // Add more supported locales
  ];
  static const String defaultLanguage = 'vi';
}
